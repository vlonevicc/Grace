# Python packages - install all at once with: pip install -r requirements.txt
# or try: pip3 install -r requirements.txt if pip is not working
openai-whisper
sounddevice
scipy
pyttsx3
openai
numpy
python-dotenv

# System dependency (install separately)
# brew install ffmpeg

# create .env file and put the key there
# OPENAI_API_KEY=your_api_key_here
# create a .gitignore file and add .env to it
